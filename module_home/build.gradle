apply from: '../module.gradle'
android {

    defaultConfig {
        if (!assembleApp.toBoolean()) {
            applicationId 'cn.dankal.address'
        }
    }

}

dependencies {
    implementation 'com.android.support.constraint:constraint-layout:+'
}
